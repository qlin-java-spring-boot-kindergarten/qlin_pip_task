pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/release' }
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = 'qlin_pip_task'
include 'src:isolation-test'
findProject(':src:isolation-test')?.name = 'isolation-test'

